# UI Components

Спецификация UI компонентов DnD Lite GM.

## Принципы

1. **Все размеры через токены** — `var(--spacing-*)`, `var(--radius-*)` и т.д.
2. **Семантические цвета** — `var(--color-danger)` вместо `#ef4444`
3. **Анимации обязательны** — hover/active/transition для интерактивных элементов
4. **Touch-friendly** — минимальный тач-таргет 44x44px
5. **Mobile-first** — кнопки `min-height: 44px` на `≤768px`
6. **Шрифт Cinzel** — для заголовков, лейблов характеристик, D&D-стилизации

---

## Мобильные компоненты (новый стиль)

### CharacterFlipCard

Прямоугольная карточка (aspect-ratio: 3/4) с CSS 3D flip-анимацией.

```
Лицевая сторона:              Обратная сторона:
┌───────────────────┐         ┌───────────────────┐
│                   │         │ [✏️] [🗑️]          │
│   (аватар или     │         │                   │
│    плейсхолдер)   │  ←тап→  │   Имя (Cinzel)    │
│                   │         │   Класс • Ур. X   │
│▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓│         │                   │
│ Имя   (Cinzel)   │         │  СИЛ ЛОВ ТЕЛ     │
│ Класс • Ур. X    │         │  ИНТ МДР ХАР     │
└───────────────────┘         │                   │
                              │  ♥ 45/60 ████░░   │
                              └───────────────────┘
```

**Props:** `character: UserCharacter`, `deletable?: boolean`, `editable?: boolean`
**Emits:** `delete`, `edit`

**CSS:** `perspective: 1000px` → `transform: rotateY(180deg)` → `backface-visibility: hidden`
**Лицевая:** аватар `object-fit: cover` + `linear-gradient(to top, rgba(0,0,0,0.75), transparent)` снизу
**Обратная:** сетка 3x2 характеристик, HP-бар с иконкой Heart, кнопки edit/delete через `v-if`

### CharacterCarousel

Свайп-карусель, показывающая одну карточку за раз.

```
         ┌───────────────────┐
         │                   │
  свайп  │  CharacterFlipCard │  свайп
  ←───── │                   │ ─────→
         └───────────────────┘
              ● ○ ○ ○           ← dots
```

**Props:** `characters: UserCharacter[]`, `readonly?: boolean`
**Emits:** `delete(id)`, `edit(id)`
**Expose:** `currentIndex` (через `defineExpose`)

- `readonly=true` → `deletable=false`, `editable=false` (для JoinSessionView)
- Трек: `translateX(-(currentIndex * 100%) + dragPercent)` через `useSwipe`
- Dots: активный с `--color-accent-primary` + `--glow-accent`, padding ≥44px

### TopBar + Sidebar (паттерн)

Переиспользуемый паттерн для мобильных страниц:

**TopBar** (48px):
- Hamburger (Menu icon, 44x44px) → emit `toggleSidebar`
- Заголовок шрифтом Cinzel

**Sidebar** (Teleport → Transition → overlay + panel):
- Overlay: `position: fixed; inset: 0; backdrop-filter: blur(4px)`
- Panel: `position: fixed; left: 0; width: min(90vw, 400px)`
- Анимация: `translateX(-100%) → 0`

Реализации:
| Компонент | Содержимое сайдбара |
|-----------|---------------------|
| `ProfileTopBar` + `ProfileSidebar` | Имя/роль, "На главную", "Выйти" |
| `PlayerLobbyTopBar` + `PlayerLobbySidebar` | Статус подключения, "Покинуть сессию" |
| `PlayerTopBar` + `PlayerSidebar` | Инфо сессии, код, статус |

---

## Кнопки

### BaseButton

| Variant | Использование | Стиль |
|---------|--------------|-------|
| `primary` | Основные действия | Filled, `--color-accent-primary` |
| `secondary` | Вторичные действия | Outlined, border |
| `ghost` | Третичные действия | Transparent |
| `danger` | Деструктивные | Red filled |

| Size | Padding | Min Height | Mobile Min Height |
|------|---------|------------|-------------------|
| `sm` | 6px 12px | 32px | 44px |
| `md` | 8px 16px | 40px | 44px |
| `lg` | 12px 24px | 48px | 48px |

**Props:** `variant`, `size`, `type`, `icon: Component`, `iconOnly`, `disabled`

---

## Модальные окна

### ConfirmModal

Переиспользуемый диалог подтверждения.

```vue
<ConfirmModal
  v-model="show"
  title="Подтверждение"
  message="Вы уверены?"
  confirm-text="Удалить"
  :danger="true"
  @confirm="handleConfirm"
/>
```

### BaseModal

Универсальная модалка с header/body/footer slots.
Размеры: `sm` (400px), `md` (560px), `lg` (800px).

---

## Карточки персонажей

### UserCharacterCard (profile/GM)

Компактная карточка с аватаром, именем, классом, характеристиками, HP.
Используется в ProfileView (NPC-секция GM) и JoinSessionView (старый стиль).

### CharacterCard (сессионный)

Карточка для сессионных персонажей в лобби GM, PlayersTab и т.д.

### CharacterFlipCard (мобильный)

Полноразмерная flip-карточка с аватаром. Используется в:
- ProfileView → карусель персонажей (edit/delete на обороте)
- JoinSessionView → выбор персонажа (readonly, без кнопок)
- PlayerLobbyView → превью персонажа (readonly, без кнопок)

---

## Toast Notifications

```typescript
const toast = useToast()
toast.success('Готово!')
toast.error('Ошибка')
```

Позиционирование: fixed, bottom-right. Slide-in анимация. Автозакрытие.

---

## Иконки (Lucide)

```vue
import { Menu, User, Pencil, Trash2, Heart, CheckCircle, Circle,
         Home, LogOut, Copy, Clock, ChevronDown } from 'lucide-vue-next'
```

| Назначение | Иконка |
|------------|--------|
| Меню (гамбургер) | `Menu` |
| Персонаж | `User` |
| Редактировать | `Pencil` |
| Удалить | `Trash2` |
| HP | `Heart` |
| Готов | `CheckCircle` |
| Не готов | `Circle` |
| На главную | `Home` |
| Выйти | `LogOut` |
| Ожидание | `Clock` |
