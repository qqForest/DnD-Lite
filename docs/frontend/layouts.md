# Layouts

Описание лейаутов интерфейса DnD Lite GM.

## Мобильные страницы (Mobile-First)

Новый паттерн для страниц вне игры: TopBar + Sidebar + контент.

### ProfileView

```
┌─────────────────────────────┐
│ [☰]  Профиль (Cinzel)       │  ← ProfileTopBar (48px)
├─────────────────────────────┤
│                             │
│  Мои персонажи (Cinzel)     │
│                             │
│  ┌───────────────────────┐  │
│  │   CharacterFlipCard   │  │  ← CharacterCarousel
│  │   (свайп ←→, тап)    │  │     (одна карточка за раз)
│  └───────────────────────┘  │
│         ● ○ ○                │  ← dots
│                             │
│   ┌─────────────────────┐   │
│   │   Новый персонаж    │   │  ← BaseButton primary
│   └─────────────────────┘   │
│                             │
│  Присоединиться к игре      │
│   ┌─────────────────────┐   │
│   │  Присоединиться     │   │  ← BaseButton primary
│   └─────────────────────┘   │
│                             │
│  --- GM секция (если GM) -- │
│  Карты: grid с UserMapCard  │
│  NPC: grid с UserCharCard   │
│  Создать сессию: кнопка     │
└─────────────────────────────┘

ProfileSidebar (slide-in):
┌─────────────────┐
│ Меню          × │
│                 │
│ Имя  [Игрок]   │
│                 │
│ 🏠 На главную   │
│ 🚪 Выйти       │
└─────────────────┘
```

### JoinSessionView

```
┌─────────────────────────────┐
│ ← Назад   Присоединиться   │  ← header (Cinzel)
├─────────────────────────────┤
│                             │
│  ┌───────────────────────┐  │
│  │   CharacterFlipCard   │  │  ← CharacterCarousel (readonly)
│  │   (свайп, тап-flip)  │  │
│  └───────────────────────┘  │
│         ● ○ ○                │
│     Выбрать персонажа       │
│                             │
│     ┌─────────────────┐     │
│     │   A B C 1 2 3   │     │  ← код сессии (monospace, uppercase)
│     └─────────────────┘     │
│                             │
│   ┌─────────────────────┐   │
│   │  Присоединиться     │   │  ← disabled без персонажа/кода
│   └─────────────────────┘   │
│                             │
└─────────────────────────────┘

Экран подключения:
┌─────────────────────────────┐
│                             │
│           ⟳                 │  ← CSS spinner
│     Подключение...          │
│                             │
└─────────────────────────────┘
```

### PlayerLobbyView

```
┌─────────────────────────────┐
│ [☰]  Лобби (Cinzel)         │  ← PlayerLobbyTopBar (48px)
├─────────────────────────────┤
│                             │
│  Ваш персонаж (Cinzel)      │
│                             │
│  ┌───────────────────────┐  │
│  │   CharacterFlipCard   │  │  ← readonly (без edit/delete)
│  │   (тап для flip)     │  │     session Character → UserCharacter map
│  └───────────────────────┘  │
│                             │
│   ┌─────────────────────┐   │
│   │    ✓ Готов          │   │  ← primary при готовности, secondary иначе
│   └─────────────────────┘   │
│                             │
│    2 из 3 игроков готовы    │  ← readyCountText
│                             │
└─────────────────────────────┘

PlayerLobbySidebar:
┌─────────────────┐
│ Меню          × │
│                 │
│ ● Подключено    │  ← connection status
│                 │
│ 🚪 Покинуть     │  ← danger button → ConfirmModal
└─────────────────┘
```

---

## GM Layout (Desktop, In-Game)

```
┌──────────────────────────────────────────────────────────────┐
│ [≡]  DnD Lite GM       ABC123  ● Подключено   4 игрока      │  ← TopBar (48px)
├──────────────────────────────────────────────────────────────┤
│  [Initiative Bar — карточки инициативы слева направо]         │  ← InitiativeBar
├─────┬────────────────────────────────────────────────────────┤
│     │                                                        │
│  👥 │                                                        │
│     │              GameMap (Konva.js)                        │
│  📜 │              зум, пан, токены                          │
│     │                                                        │
│  ⚔️ │                                                        │
│     │                                                        │
├─────┴────────────────────────────────────────────────────────┤
│  [🎲 Бросок]  [Последний: d20 → 17]                          │  ← BottomBar
└──────────────────────────────────────────────────────────────┘
  Left Panel (expandable): PlayersTab, CombatTab
```

```css
.gm-layout {
  display: grid;
  grid-template-areas:
    "top    top"
    "init   init"
    "left   main"
    "bottom bottom";
  grid-template-columns: auto 1fr;
  grid-template-rows: 48px auto 1fr 64px;
  height: 100vh;
  overflow: hidden;
}
```

---

## Player Layout (In-Game)

```
┌──────────────────────────────┐
│ [☰]  ABC123  ● Подключено   │  ← PlayerTopBar (48px)
├──────────────────────────────┤
│                              │
│         GameMap              │  ← Konva.js (readonly tokens)
│         (full area)          │
│                              │
├──────────────────────────────┤
│ [Персонаж]  HP: ████░ 45/60 │  ← Bottom info
│ [🎲 Бросок]                  │
└──────────────────────────────┘

PlayerSidebar (slide-in):
┌──────────────────┐
│ Меню           × │
│                  │
│ Код: ABC123      │
│ Игроков: 4       │
│ Статус: Игра     │
└──────────────────┘
```

```css
.player-layout {
  display: grid;
  grid-template-areas:
    "top"
    "main"
    "bottom";
  grid-template-rows: 48px 1fr auto;
  height: 100vh;
  height: 100dvh;
  overflow: hidden;
}
```

---

## GM Lobby

```
┌──────────────────────────────┐
│  Лобби GM                    │
│                              │
│  Код сессии: [ABC123] [📋]   │  ← SessionCodeDisplay
│                              │
│  Подключенные игроки:        │
│  ┌──────────────────────────┐│
│  │ ● Торин    ✓ Готов      ││  ← PlayersLobbyList
│  │ ● Леголас  ⏳ Не готов  ││     (expandable cards)
│  └──────────────────────────┘│
│                              │
│  Карты: [grid]               │
│  NPC: [grid]                 │
│                              │
│  [Начать игру]               │
└──────────────────────────────┘
```

---

## Responsive Behavior

| Breakpoint | Поведение |
|------------|-----------|
| `≤480px` | Грид карт/NPC: 1 колонка |
| `≤768px` | Грид: 2 колонки. Кнопки: min-height 44px |
| `≤1024px` | GM Layout: панель в overlay |
| `≥1024px` | Полный десктоп layout |

**Mobile-first подход:** базовые стили для мобильных, `@media (min-width)` для десктопа.
